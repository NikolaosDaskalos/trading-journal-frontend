import { MenuItem } from './interfaces';

export const usersMenu: MenuItem[] = [
  { text: 'Register', path: 'user/register' },
  { text: 'Sign-in', path: 'user/sign-in' },
];

export const tradesMenu: MenuItem[] = [];
